experiment_name: "run_multimodal_rag"
output_dir: ./outputs/${experiment_name}

hydra:
  run:
    dir: ${output_dir}

version: 1.1.0
defaults:
  - _self_

logging:
  file: ${output_dir}/run_hydra.log

device:
  use_cuda: auto
  seed: 123

